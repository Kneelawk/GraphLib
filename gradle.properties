# Gradle Properties
org.gradle.jvmargs = -Xmx1G
org.gradle.parallel = true

# Mod Properties
mod_version = 0.1.0+1.18.2
maven_group = com.kneelawk
archives_base_name = wirenetlib

# Dependencies are managed at gradle/libs.versions.toml